import Layout from "../components/Layout";
import TripForm from "../components/TripForm";

const NewTrip = () => {
    return (
        <Layout>
            return <TripForm />;
        </Layout>
    );
};

export default NewTrip;
